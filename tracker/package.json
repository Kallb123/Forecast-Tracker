{
  "name": "forecast-tracker",
  "version": "1.0.0",
  "description": "Daily weather forecast ingestion to InfluxDB 2 for forecast consistency tracking",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "start:ui": "node src/server.js",
    "test": "node --test",
    "smoke": "node -e \"const {runCollectionCycle} = require('./src/index'); runCollectionCycle().then(() => process.exit(0)).catch(e => { console.error(e); process.exit(1); })\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2"
  }
}
